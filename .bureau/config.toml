[runtime]
language    = "dotnet"
base_image  = "mcr.microsoft.com/dotnet/sdk:10"
install_cmd = "dotnet restore"
lint_cmd    = "dotnet format src/BureauObservability.sln --verify-no-changes"
build_cmd   = "dotnet build src/BureauObservability.sln"
test_cmd    = "dotnet test src/BureauObservability.sln"

[ralph_loop]
max_builder_attempts = 3
max_rounds           = 3
command_timeout      = 300

[bureau]
builder_model   = "claude-sonnet-4-6"
reviewer_model  = "claude-opus-4-7"
